// C++ code
//
const int potPin = A0;
const int motorPin = 9;

void setup()
{
pinMode(motorPin, OUTPUT);
}

void loop()
{
int potValue = analogRead(potPin);
//This converts to PWM range
int speedValue = map(potValue, 0, 1023, 0, 255);

analogWrite(motorPin, speedValue);
}
